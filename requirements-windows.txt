# Windows (B-layer): no mlx-whisper.
# Also install ffmpeg (PATH, YTJ_FFMPEG, or repo bin/ffmpeg.exe).
#   winget install Gyan.FFmpeg
#
# Prefer Python 3.12 **Windows installer (64-bit / AMD64)** from python.org —
# even on Snapdragon / Windows on ARM (runs under x64 emulation). ARM64 Python
# can install torch, but torchaudio wheels are often missing.
#
# Prefer: powershell -ExecutionPolicy Bypass -File scripts\install_windows.ps1
--extra-index-url https://download.pytorch.org/whl/cpu

faster-whisper==1.1.1
torch==2.9.1
# torchaudio is installed by install_windows.ps1 on AMD64; optional on ARM64.
soundfile==0.13.1
numpy==2.2.6
scikit-learn==1.6.1
speechbrain==1.1.0
transformers==4.57.6
sentencepiece==0.2.2
sacremoses==0.1.1
