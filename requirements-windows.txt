# Windows (B-layer): no mlx-whisper.
# Also install ffmpeg (PATH, YTJ_FFMPEG, or repo bin/ffmpeg.exe).
#   winget install Gyan.FFmpeg
# CPU default. NVIDIA: install a CUDA torch wheel first, then this file.
#
# Use THIS file on Windows — not requirements.txt (that one pins mlx-whisper + older torch).
# Prefer Python 3.11–3.12 from python.org. Python 3.13 needs newer wheels (below).
# Pins chosen so pip installs binary wheels (no Visual Studio C++ build).
faster-whisper==1.1.1
torch==2.9.1
torchaudio==2.9.1
soundfile==0.13.1
numpy==2.2.6
scikit-learn==1.6.1
speechbrain==1.1.0
transformers==4.57.6
sentencepiece==0.2.2
sacremoses==0.1.1
