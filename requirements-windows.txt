# Windows (B-layer): no mlx-whisper.
# Also install ffmpeg (PATH, YTJ_FFMPEG, or repo bin/ffmpeg.exe).
#   winget install Gyan.FFmpeg
#
# Use THIS file on Windows — not requirements.txt (mlx-whisper + Mac torch pins).
# Prefer Python 3.11–3.12 x86_64 from python.org (64-bit).
#
# PyTorch CPU wheels are also published on download.pytorch.org; include that index
# so pip does not report "from versions: none" when the default index is incomplete.
--extra-index-url https://download.pytorch.org/whl/cpu

faster-whisper==1.1.1
torch==2.9.1
torchaudio==2.9.1
soundfile==0.13.1
numpy==2.2.6
scikit-learn==1.6.1
speechbrain==1.1.0
transformers==4.57.6
sentencepiece==0.2.2
sacremoses==0.1.1
